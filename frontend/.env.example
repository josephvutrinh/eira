# Supabase
# Create a project in Supabase and copy these values from Project Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: if you deploy a Supabase Edge Function named "delete-account",
# the client will call it when you press "Delete Account".
# (Using supabase.functions.invoke('delete-account'))
